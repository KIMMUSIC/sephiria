# 배포 안내 (Vercel)

## 결론

**Vercel 배포 가능하다. Hobby(무료) 플랜으로 충분하다.**

이 앱은 페이지가 전부 정적으로 미리 렌더되고, 무거운 계산(최적 배치 탐색, 스크린샷 인식)은
전부 브라우저의 Web Worker 안에서 돈다. 서버가 하는 일은 문의 폼을 웹훅으로 중계하는
함수 하나뿐이다.

## 근거 (프로덕션 빌드에서 실측)

| 항목 | 실측값 |
|---|---|
| 라우트 | `○ /` 정적 · `ƒ /api/feedback` 함수 · `○ /icon.svg` · `○ /apple-icon.png` |
| 첫 로드 JS | 173 kB (`/` 페이지 87.5 kB + 공유 87.2 kB) |
| 최적화 워커 | 프로덕션 청크로 분리되어 200 응답, 23,022회 반복 완료 |
| 인식 워커 | 템플릿 로드 시 `/images/**` **222건 전부 200**, '준비 완료' 도달 |
| 정적 이미지 | 315개 PNG, 합계 479 KB (`public/images/`) |
| 추적 파일 총량 | 5.2 MB |
| 필요한 런타임 환경변수 | `FEEDBACK_WEBHOOK_URL` 하나 (없어도 앱은 정상 동작) |

Web Worker가 프로덕션 번들에서 살아남는지가 이 앱 배포의 유일한 실질 위험이었는데,
`next build` + `next start` 로 직접 확인했다. 두 워커 모두 정상 동작한다.

## 배포 절차

1. GitHub 저장소를 Vercel 에 연결한다. 프레임워크는 Next.js 로 자동 감지되고
   빌드 명령·출력 디렉터리를 따로 설정할 필요가 없다.
2. 환경변수를 넣는다 (아래 표).
3. 배포한다. 첫 배포 후 도메인이 정해지면 `NEXT_PUBLIC_SITE_URL` 을 그 도메인으로 채운다.

## 환경변수

| 이름 | 필수 | 설명 |
|---|---|---|
| `FEEDBACK_WEBHOOK_URL` | 문의 기능을 쓰려면 필수 | Discord 또는 Slack 의 Incoming Webhook URL. 서버에서만 읽으므로 방문자에게 노출되지 않는다. |
| `NEXT_PUBLIC_SITE_URL` | 선택 | Open Graph 절대 URL 기준값. 비우면 Vercel 이 주는 `VERCEL_URL` 을, 그것도 없으면 `http://localhost:3000` 을 쓴다. |

`FEEDBACK_WEBHOOK_URL` 이 없으면 문의 전송이 503 과 함께
"문의 채널이 아직 설정되지 않았습니다" 를 돌려준다. 앱의 나머지 기능은 영향받지 않는다.

Discord 웹훅 만드는 법: 채널 설정 → 연동 → 웹후크 → 새 웹후크 → URL 복사.
Slack 이면 Incoming Webhooks 앱에서 발급받은 URL 을 그대로 넣으면 된다.
본문을 `{ content, text }` 두 필드로 함께 보내므로 둘 중 어느 쪽이든 그대로 동작한다.

## 문의 폼이 익명인 이유와 방식

방문자는 페이지 안에서 바로 쓰고, 어디로 가는지 알 수 없어야 한다는 요구에 맞췄다.

- 브라우저는 같은 출처의 `/api/feedback` 에만 요청한다. 웹훅 URL 은 서버에만 있고
  응답 어디에도 실리지 않는다. 개발자 도구를 열어도 목적지를 알 수 없다.
- 전송 내용은 `[세피리아 최적화 · {유형}]\n{내용}` 이 전부다. IP, User-Agent, 쿠키,
  시각 중 어느 것도 붙이지 않는다.
- 숨은 허니팟 필드가 채워지면 웹훅을 부르지 않고 200 을 돌려준다. 봇에게 차단 사실을
  알리지 않기 위해서다.

### 남아 있는 위험

공개 엔드포인트라 길이 제한과 허니팟만으로는 작정한 스팸을 막지 못한다. 실제로 남용이
발생하면 Vercel 대시보드의 Firewall 에서 `/api/feedback` 에 rate limit 을 걸거나
Attack Challenge Mode 를 켜는 것이 가장 손이 덜 간다. 코드 변경 없이 처리된다.

## 알아둘 점

- `data/vision/embed-index.json` (2.9 MB) 는 저장소에서 가장 큰 파일이지만 앱 코드가
  참조하지 않는다. 스크립트가 쓰는 빌드 산출물이라 클라이언트로 나가지 않는다.
- 첫 방문자는 인식 템플릿 때문에 스프라이트 300여 개를 받는다(합계 약 0.5 MB).
  Vercel 의 정적 자산 CDN 캐시가 붙으므로 재방문에는 다시 받지 않는다.
- `next.config.mjs` 의 보안 헤더(`X-Frame-Options`, `X-Content-Type-Options`,
  `Referrer-Policy`)는 Vercel 에서 그대로 적용된다.
- Node 버전을 고정하고 싶으면 `package.json` 에 `"engines": { "node": "22.x" }` 를 넣는다.
  넣지 않으면 Vercel 기본값을 따르며, 현재 기본값에서 빌드가 통과하는 것을 확인했다.
- `.env.local` 의 `OPENAI_API_KEY` 는 앱 코드 어디에서도 참조하지 않는 잔재다.
  Vercel 에 넣을 필요가 없다.
