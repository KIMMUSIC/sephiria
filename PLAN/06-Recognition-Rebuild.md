# 06 — 인벤토리 인식 재구축 계획

> 작성일 2026-08-09 · 대상 `D:\세피리아` · 상태: 계획(미실행)
> 이 문서의 모든 수치는 **실측값**이다. 재현 스크립트는 §2.6 참조.

---

## 1. 결론 요약

이전 인식률이 0%에 가까웠던 것은 튜닝 부족이 아니라 **매칭 수식 자체가 정답을 임계값 위로 올릴 수 없는 구조**였기 때문이다. 파라미터를 아무리 조정해도 개선되지 않는 종류의 결함이다.

핵심 근거 한 줄: 정답이 확실한 셀에서 **정답 후보의 점수가 0.4137인데 통과 임계값이 0.60**이었고, 그 정답은 295개 후보 중 **37위**였다. 즉 임계값을 낮춰도 오답이 먼저 나온다.

같은 이미지에 대해 올바른 방식(전경 분리 + 알파 마스크 매칭)을 100줄 남짓으로 프로토타이핑하자 **빈칸 판정 14/14 정확**, 아이템 top-1 약 **65%**가 튜닝 없이 나왔다. 남은 오차의 상당수는 알고리즘이 아니라 **스프라이트 카탈로그 누락**(신규 아이템 30종·석판 7종이 로컬에 없음)으로 설명된다.

**목표 재정의(중요):** "100% 인식"은 임의 스크린샷에 대한 보장으로는 비현실적이다. 대신 다음을 목표로 한다.
- 원본 해상도 PNG 스크린샷: **top-1 99% 이상**
- 그 외(JPEG·리사이즈·스케일링): **95% 이상 + 신뢰도 기반 확인 UI**로 실질 100% 도달

이 구조가 필요한 이유는 §7에 있다.

---

## 2. 왜 0%였는가 — 측정된 근본 원인

심각도 순. ①이 단독으로 0%를 설명하며, 나머지는 ①을 고쳐도 남는 결함이다.

### ① 히스토그램 정규화 비대칭 — 정답이 임계값을 넘는 것이 수학적으로 불가능

`lib/templateMatcher.ts`의 참조 템플릿과 셀은 **서로 다른 모집단으로 정규화**된다.

| 대상 | 코드 | 정규화 모집단 |
|---|---|---|
| 참조 스프라이트 | `computeHistogram(imgData, hasAlpha=true)` (L48) | **불투명 픽셀만** (`a < 128` 건너뜀, L93) |
| 인벤토리 셀 | `computeHistogram(data, false)` (L143) | **배경 포함 전체 픽셀** |

히스토그램 교집합은 `Σ min(a_i, b_i)`이므로, 셀 히스토그램 질량 중 배경이 차지하는 비율만큼은 **어떤 템플릿과도 겹칠 수 없다**. 따라서

```
달성 가능한 최대 점수 ≈ 셀에서 아이콘이 차지하는 면적 비율
```

실측: 템플릿 불투명 픽셀 비율 **평균 26.0% / 중앙값 24.2%**, 셀 픽셀의 **68~88%가 배경**.
→ 완벽히 일치해도 상한이 약 0.3~0.5인데 `MATCH_THRESHOLD = 0.60`. **정답은 정의상 통과 불가.**

### ② 색 히스토그램은 픽셀아트에서 형태 정보를 전부 버린다

세피리아 아이콘은 팔레트가 극히 작다(`humility_crown` 불투명 영역 **고유색 7개**). 히스토그램은 공간 배치를 버리므로, 팔레트가 비슷한 전혀 다른 아이템이 동일 점수를 받는다. 실측에서 정답 `humility_crown`(0.4137) 위에 `lerids_battleb_racelet`(0.5470), `single_tree_charcoal`(0.5039) 등 **무관한 아이템 5개**가 먼저 왔다.

### ③ `vision.worker.ts`의 템플릿 센터크롭이 아이콘을 잘라낸다

```ts
const TMPL_SIZE = 48
if (cols >= TMPL_SIZE && rows >= TMPL_SIZE) {
  const cx = Math.floor((cols - TMPL_SIZE) / 2)
  tplMat = rgb.roi(new cv.Rect(cx, cy, TMPL_SIZE, TMPL_SIZE)).clone()  // resize가 아니라 crop
}
```

스프라이트는 **80×80 또는 100×100**(실측, 파일마다 다름). 이 코드는 리사이즈가 아니라 **크롭**이므로 80×80에서는 바깥 40%, 100×100에서는 면적의 77%를 버린다. 게다가 셀은 64×64로 리사이즈되어 들어오는데 템플릿은 원본 해상도 조각이라 **스케일이 불일치**한다. `TM_CCOEFF_NORMED`는 스케일 불변이 아니다.

### ④ 마스킹이 아이콘 본체를 지운다

`prepareCellForMatching()`은 `V < 70` 픽셀, **상단 18%**, **하단 15%**, 상단 양 모서리를 검게 칠한다.
- 세피리아 아이콘 상당수가 어두운 색(검은 비늘·회색 돌·어두운 검)이라 `V < 70`에 **아이콘 자체가 삭제**된다.
- 상하 33% 제거는 실제 아이콘 픽셀을 잘라낸다.
- 양쪽 모두 검게 칠한 뒤 `TM_CCOEFF_NORMED`를 돌리면 상관계수가 **검은 영역끼리의 일치**에 지배되어, 검은 영역이 많은 템플릿이 무조건 이긴다.

이 실패 양상은 프로토타입에서도 재현되었다: 마스크 MAE만 쓴 v1에서 `single_tree_charcoal`이 **36칸 중 20칸에서 1위**를 차지했다. 작고 어두운 템플릿이 어두운 배경 위에서 항상 유리한 편향이다.

### ⑤ 아티팩트와 석판을 같은 후보 풀에서 경쟁시킨다

`matchCell()`은 ARTIFACT 295개와 TABLET을 한 배열에 넣고 최대값을 취한다. 두 계층은 렌더링 방식도 배경도 다르므로 서로의 오답원이 된다.

### ⑥ 그리드 검출이 취약

`autoDetectGrid()`는 `l>20 && l<100 && b>=g && r<100` 색 마스크 + 행/열 밀도 투영에 의존하고 `cols: 6`을 하드코딩한다. 실제 스크린샷은 셀 배경이 두 종류(석판 있는 칸 = 청회색, 빈 칸 = 적갈색)이고 바깥에 갈색 프레임이 있어 밀도 투영이 프레임까지 함께 잡는다. `computeCellCrops()`는 균등 분할이라 원점 오차가 6열에 걸쳐 누적된다.

### 2.6 재현 방법

```
scratchpad/repro.py    # ①②의 실측 재현 (정답 0.4137 / 37위 / 커버리지 통계)
scratchpad/proto2.py   # 제안 방식 프로토타입 + 육안 검증 시트 생성
```

---

## 3. 실증 — 고치면 되는가

동일 스크린샷(`인벤 예시/2.png`, 6×6 = 36칸)에서:

| 방식 | 빈칸 판정 | 아이템 top-1 | 비고 |
|---|---|---|---|
| 현행 코드 (히스토그램) | — | **0%** | 정답 37위, 0.4137 < 0.60 |
| v2 전경분리 + 형태 IoU | **14/14 (100%)** | **약 65%** | 무튜닝, 100줄 |

v2 오답 6건 중 다수(초록 X자 석판, 청·주황 돔형, 초록 기둥 등)는 **로컬에 스프라이트가 없는 신규 아이템일 가능성이 높다**. 즉 알고리즘 오차와 카탈로그 결손이 섞여 있다. 카탈로그를 채우면 실측 정확도는 더 올라간다.

핵심 통찰: **이것은 자연영상 인식이 아니라 결정론적 2D 렌더링의 역문제**다. 게임은 동일 스프라이트를 고정 규칙으로 합성해 그린다. 따라서 렌더링을 재현해 비교하면(analysis-by-synthesis) 원리적으로 거의 정확히 맞출 수 있다. 프로토타입 v3에서 일부 석판은 2위와의 격차가 **+0.53**까지 벌어졌다.

---

## 4. 목표 아키텍처

```
스크린샷
  │
  ├─ A. 그리드 캘리브레이션   셀 격자·피치·원점을 서브픽셀로 확정
  │
  ├─ B. 셀 분해              칸별 크롭 + UI 오버레이 영역 분리
  │
  ├─ C. 배경 모델링          칸 유형 판정(석판칸 / 빈칸) + 배경 플레이트 추정
  │
  ├─ D. 점유 판정            배경 대비 잔차로 빈칸/아이템칸 이진 분류
  │
  ├─ E. 정합 매칭            알파 마스크 기반, 스케일·오프셋 캘리브레이션 후 고정
  │
  ├─ F. 신뢰도 산정          1위·2위 격차 기반 확신도, 저확신은 후보 3개 제시
  │
  └─ G. 사용자 확정 UI        저확신 칸만 사람이 1클릭 수정 → 실질 100%
```

설계 원칙 4가지:

1. **비대칭 정규화 금지.** 셀과 템플릿은 항상 동일 모집단(템플릿 알파 마스크 내부)에서만 비교한다.
2. **형태를 버리지 않는다.** 히스토그램은 후보 선별(prefilter)에만 쓰고, 최종 판정은 반드시 공간 정합으로 한다.
3. **스케일을 추정하지 말고 캘리브레이션한다.** 한 스크린샷 안에서 아이콘 렌더 스케일은 상수다. 확신도 높은 칸 몇 개로 스케일·오프셋을 1회 추정해 **전 칸에 고정 적용**한다. 이것이 정확도를 크게 올리는 지렛대다.
4. **불확실성을 숨기지 않는다.** 억지로 답을 내지 말고 저확신을 표면화해 G단계로 넘긴다.

---

## 5. 실행 계획

### Phase 0 — 안전 및 기반 (선행, 차단 요소)

- **0-1. 노출된 OpenAI API 키 폐기.** `.env.local`에 유효한 `sk-proj-...` 키가 평문으로 있다. 즉시 재발급하고 기존 키를 폐기할 것. §8 참조.
- **0-2. 정확도 측정 하네스 구축.** 이후 모든 작업은 이 숫자로 판정한다. 프로토타입 스크립트를 프로젝트 내 `tests/vision/`로 승격.
- **0-3. 정답 라벨 완성.** `tests/fixtures/*.expected.json`은 현재 대부분 `"???"`인 초안이다. 5장 전부를 확정 라벨로 채운다. **이것이 없으면 개선 여부를 알 수 없다.**

> 검증: `1.jpeg`~`5.png` 5장에 대해 현행 코드 기준선 정확도가 숫자로 출력될 것.

> **Phase 0 상태: 완료 (2026-08-09).** 결과는 §6, 실행 중 확인된 새 사실은 §10 참조.
> 남은 항목은 0-1(API 키 폐기, 사용자 조치)과 `1.jpeg` 정답 라벨뿐이다.

### Phase 1 — 카탈로그 동기화

> **1-2, 1-3은 Phase 0에서 이미 완료했다.** 라벨링을 하려면 신규 아이템 스프라이트가 반드시 필요했기 때문이다.
> - 신규 스프라이트 37종(아티팩트 30 + 석판 7) 다운로드 완료 — 전부 HTTP 200
> - 개명 5건의 구 파일은 `public/images/_deprecated/`로 격리 (5쌍 모두 픽셀 동일 확인 → 개명임이 확정됨)
> - 로컬 카탈로그가 위키와 정확히 일치: **아티팩트 267 / 석판 60**
> 남은 것은 1-1(`data/*.json` 동기화 스크립트)과 1-4(신규 석판 7종 한글명)다.

위키가 RSC 페이로드에 **전체 데이터셋을 JSON으로 임베드**하고 있음을 확인했다. CDN 스프라이트도 직접 받을 수 있다.

- **1-1. 동기화 스크립트 작성.** `https://www.sephiria.wiki/artifact` HTML에서 레코드 배열을 파싱해 `data/artifacts.json` 갱신. 확장자는 `.webp` → `.png` 순으로 폴백(신규 항목은 `.png`만 있는 경우 확인됨).
- **1-2. 신규 스프라이트 수급.** 아티팩트 **30종**, 석판 **7종**을 `img.sephiria.wiki`에서 내려받아 `public/images/`에 추가. (전부 HTTP 200 확인 완료)
- **1-3. 개명 처리.** 아래 5건은 삭제가 아니라 rename이므로 매핑 테이블로 흡수한다.

| 기존 | 변경 |
|---|---|
| `broken_root` | `broken` |
| `calges` | `calges_2` |
| `green_ink_bottle_v2` | `green_ink_bottle` |
| `lightning_boomerang` | `lightningboomerang` |
| `home_town` | `home-town` |

- **1-4. 신규 석판 7종의 한글명 확보** — `defender` `development` `flag` `junction` `last_stand` `rally`, 그리고 `home-town`. 위키 정적 HTML에는 없어 클라이언트 렌더링 경로 확인 필요.

> 검증: 아티팩트 267종·석판 60종 전부에 대해 로컬 스프라이트 파일이 존재하고 열림.

### Phase 2 — 그리드 캘리브레이션 재작성

현행 두 경로(`lib/gridDetector.ts` 색마스크, `workers/vision.worker.ts` 컨투어)를 **하나로 통합**한다.

- **2-1.** 셀 테두리의 주기성을 이용한 격자 추정으로 전환. 색 임계값이 아니라 **행/열 방향 자기상관(autocorrelation)으로 셀 피치를 구하고**, 피치에 위상을 맞춰 원점을 정한다. 두 종류 배경(석판칸/빈칸)에 모두 강건하다.
- **2-2.** 균등 분할 누적 오차 제거 — 셀마다 개별 좌표를 갖게 한다.
- **2-3.** 열 수는 하드코딩하지 말고 추정하되 6을 사전확률로 사용.

> 검증: ~~5장 전부에서 검출 격자와 수작업 격자의 셀 중심 오차 ≤ 2px.~~
> **이 기준은 폐기했다** — 픽스처 격자가 같은 방법으로 만들어져 순환이며, 실측에서 px 오차와 실제 정확도가 역상관이었다. 대체 기준: **자동 격자의 end-to-end 인식 정확도가 손라벨 격자 이상.** §9-A 참조. (달성)

### Phase 3 — 매칭 엔진 교체 (핵심)

`lib/templateMatcher.ts`의 히스토그램 경로를 **폐기**하고 다음으로 대체한다.

- **3-1. 템플릿 전처리 재작성.** 알파 바운딩박스로 **타이트 크롭 후 정사각 패딩**(현행 센터크롭 폐기). 80/100px 혼재 문제가 여기서 사라진다. 팔레트 PNG(`mode=P`) 파일이 섞여 있으므로 RGBA 변환을 강제.
- **3-2. 배경 플레이트 추정.** 칸 외곽 링에서 지배색을 추출해 배경 모델 구성.
- **3-3. 점유 판정.** 배경 대비 잔차 비율로 빈칸 분류. *프로토타입에서 이미 14/14 달성.*
- **3-4. 2단계 매칭.**
  - 1단계: 축소 해상도 정합으로 상위 후보 선별 — 단, **단일 스케일 prefilter는 금지**(프로토타입에서 정답이 탈락해 정확도가 무너진 실패 사례 있음). 복수 스케일에서 선별하고 넉넉히 남길 것.
  - 2단계: 알파 마스크 내부에서만 형태 IoU + 색 일치도 결합 점수.
- **3-5. 스케일·오프셋 캘리브레이션.** §4 원칙 3. 고확신 칸으로 렌더 스케일을 추정해 전 칸 고정.
- **3-6. 편향 보정.** 작고 어두운 템플릿이 유리해지는 편향을 정규화로 제거(마스크 크기 정규화 + 배경 가설 대비 상대점수).
- **3-7. 석판/아티팩트 후보 풀 분리.**
- **3-8. 회전 처리.** `rotate: true` 석판만 4방향 평가.

> 검증: 5장 합산 top-1 ≥ 95%, 빈칸 판정 ≥ 99%.

### Phase 4 — 신뢰도와 사용자 확정 UI

- **4-1.** 1위·2위 점수 격차를 신뢰도로 환산해 임계값 보정.
- **4-2.** 저확신 칸을 그리드에 하이라이트하고 후보 3개를 제시해 1클릭 수정.
- **4-3.** 수정 결과를 회귀 픽스처로 축적하는 경로 마련.

> 검증: 사용자 개입 후 최종 정확도 100%에 도달 가능하며, 개입 필요 칸이 5% 이하.

### Phase 5 — 정리

- **5-1.** 중복 인식 경로 제거. 현재 `ImageUploader`(메인스레드 히스토그램)와 `SmartUploader`(OpenCV 워커) 두 계통이 공존한다. 하나만 남긴다.
- **5-2.** OpenCV.js 의존 재평가. `eval()`로 워커에 주입하는 현행 방식은 취약하다. 새 파이프라인은 순수 Canvas/TypedArray로 구현 가능하므로 **OpenCV 제거를 우선 검토**한다(번들 크기·초기화 실패 리스크 동시 해소).

---

## 6. 검증 체계

측정 없이 진행하지 않는다. 각 Phase는 아래 지표로 판정한다.

`npm run test:vision` 으로 언제든 재측정한다. 아래 기준선은 **Phase 0에서 실제로 측정된 값**이다.

| 지표 | 기준선(현행 알고리즘) | 목표 |
|---|---|---|
| 빈칸 판정 정확도 | **71.2%** | ≥ 99% |
| 아이템 top-1 정확도 (전체) | **12.5% (6/48)** | ≥ 99% |
| 아이템 top-1 정확도 (JPEG `1.jpeg`) | 측정 불가 (정답 미확정) | ≥ 95% |
| 타입(아티팩트/석판) 정확도 | 93.8% | ≥ 99% |
| 종합 정확도 | 59.6% (62/104) | ≥ 99% |
| 사용자 개입 필요 칸 비율 | — | ≤ 5% |

이미지별 기준선:

| 이미지 | 채점 | 스킵 | 빈칸 | top-1 | 종합 |
|---|---|---|---|---|---|
| 1.jpeg | 0 | 30 | — | — | — |
| 2.png | 31 | 5 | 74.2% | 13.3% (2/15) | 58.1% |
| 3.png | 22 | 14 | 90.9% | 40.0% (2/5) | 86.4% |
| 4.png | 28 | 8 | 78.6% | 10.0% (1/10) | 67.9% |
| 5.png | 23 | 13 | 39.1% | 5.6% (1/18) | 26.1% |
| **합계** | **104** | **70** | **71.2%** | **12.5%** | **59.6%** |

> **§1의 "0%" 표현 정정.** 격자를 정확히 맞추기 전(이미지 전체를 격자로 간주하던 상태)에는 top-1이 실제로 0/21이었다. Phase 0에서 격자를 캘리브레이션하자 **12.5%** 로 올라갔다. 즉 초기 0%는 *히스토그램 결함 + 격자 오정렬*의 합이었고, 격자를 고쳐도 12.5%에 그친다는 사실이 §2①의 구조적 결함을 오히려 더 분명히 보여준다. 앞으로 비교 기준은 **12.5%** 다.

---

## 7. 왜 "무조건 100%"를 약속하지 않는가

- `인벤 예시/1.jpeg`는 **JPEG**이다. 손실 압축이 픽셀아트의 팔레트를 훼손한다.
- 사용자가 임의 배율로 캡처·리사이즈하면 보간으로 원본 팔레트가 소실된다.
- 시각적으로 거의 동일한 아이템 쌍이 존재할 수 있다(배지 계열 `diamond_badge`·`garnet_badge`·`topaz_badge` 등 색만 다른 세트가 실제로 신규 추가됨).
- 게임이 또 업데이트되면 카탈로그가 다시 어긋난다 → Phase 1 동기화의 상시화가 필요.

따라서 **높은 자동 정확도 + 저확신 칸 확인 UI**의 조합으로 실질 100%를 만든다. 이 편이 "100% 자동"을 주장하다 조용히 틀리는 것보다 안전하다.

---

## 8. 보안 — 즉시 조치 필요

`D:\세피리아\.env.local`에 **유효한 OpenAI API 키가 평문으로 저장**되어 있다.

```
OPENAI_API_KEY=sk-proj-...(생략)
```

확인된 사실:
- `.gitignore`에 `.env.local`이 포함되어 있고, 이 디렉터리는 git 저장소가 아니다 → **원격에 커밋된 적 없음.** 유출 범위는 로컬에 한정된다.
- 다만 이 키는 계획 수립 과정에서 **에이전트 컨텍스트에 평문으로 읽혔다.**

권고:
- 위험도는 "긴급"이 아니라 "정리 대상"이다. 그래도 **폐기 후 재발급**을 권한다 — 비용이 거의 없고 불확실성을 없앤다.
- 향후 Vision API를 쓴다면 키는 서버 라우트에서만 사용하고 클라이언트 번들에 유입되지 않게 할 것. (`app/api/analyze-inventory/`는 현재 빈 디렉터리다 — 이 경로를 쓰려던 흔적으로 보인다.)

---

## 9. 미해결 항목

계획 실행 전 확정이 필요하지만, **인식 파이프라인 자체를 막지는 않는** 항목들이다.

- **셀 좌상단 `"2/3"` `"0/4"` `"2/7"` 숫자의 의미 — 미확정.** 정찰 결과는 "슬롯 좌표(row/col)"라고 보고했으나 **반증됨**: 로컬 `slotToKey()`는 하이픈(`row-col`)을 쓰고, 6열 격자에서 `"2/7"`·`"5/5"` 같은 값이 나올 수 없으며, 아이콘이 있어도 숫자가 없는 칸이 존재한다. 실제 의미는 재확인 필요.
- 빈칸의 `"+1"` `"-1"` `"+2"` 오버레이 의미 — 위 항목과 함께 재확인 필요.
- 신규 석판 7종의 한글명.
- 위키 석판 총 60종 여부 — 신규 7종의 CDN 존재는 확인했으나 전체 수는 독립 재검증 안 됨.

이들은 **옵티마이저·효과 계산**에 필요하며 이미지 인식에는 영향이 없다. 인식 작업과 병행해 확정하면 된다.

---

## 9-A. Phase 3 실측 결과 (2026-08-09)

레퍼런스 구현(`tests/vision/REFERENCE.py`)으로 픽스처를 채점한 결과다. 목표에는 못 미쳤다.

> **아래 수치는 라벨 48개 시절의 것이다. 라벨을 86개로 늘린 뒤의 최신 수치는 §9-C 를 보라.**

| 지표 | 기준선 | Phase 3 | 목표 |
|---|---|---|---|
| 빈칸 판정 | 71.2% | **98.1%** | ≥ 99% |
| 아이템 top-1 | 12.5% (6/48) | **70.8% (34/48)** | ≥ 99% |
| 종합 | 59.6% | **84.6%** | ≥ 99% |

이미지별로 편차가 크다. `2.png` 93.3%, `3.png` 80.0%인 반면 `4.png` 60.0%, `5.png` 55.6%다.

TypeScript 포팅(`lib/vision/plate-matcher.ts`)은 레퍼런스 수치를 **정확히 재현**했다. `npm run test:vision` 이 baseline 과 plate-matcher 를 나란히 출력한다.

### Phase 2 격자 자동 검출 — 성공

`lib/vision/grid-calibrate.ts`. **완전 자동 격자 검출이 손라벨 격자와 동등하거나 더 낫다.** 판정 지표는 셀 중심 px 오차가 아니라 **end-to-end 인식 정확도**다(이유는 아래).

| 격자 출처 | 빈칸 | top-1 | 종합 |
|---|---|---|---|
| 손라벨 (대조군) | 98.1% | 70.8% (34/48) | 84.6% (88/104) |
| **자동 + 매칭 스코어러** | **100.0%** | **70.8% (34/48)** | **86.5% (90/104)** |
| 자동, 스코어러 없음(밝기 폴백) | 99.0% | 58.3% (28/48) | 80.8% (84/104) |

**매칭 스코어러가 top-1 을 12.5%p 끌어올린다.** 밝기 프로파일만으로는 부족하다는 것이 측정으로 확인됐다.

### 행 수 자동 검출 — 4/5

`rows` 를 주지 않고 `calibrateGrid(img, {cols: 6, scorer})` 를 호출했을 때:

| 이미지 | 실제 행 | 자동+스코어러 | 폴백(스코어러 없음) |
|---|---|---|---|
| 1.jpeg | 5 | **5** | 4 ✗ |
| 2.png | 6 | **6** | 4 ✗ |
| 3.png | 6 | **6** | 4 ✗ |
| 4.png | 6 | **4 ✗** | 4 ✗ |
| 5.png | 6 | **6** | 4 ✗ |

초기 구현은 **5장 전부 3행**을 반환했다(0/5). 원인은 축 비용이 n 에 대해 비교 불가능했던 것 — `gapSum/(n-1)` 은 n-정규화되지만 `dimmest` 는 n개 셀에 대한 min 이라 n 이 커질수록 단조 감소한다. 행 수를 **축 비용이 아니라 완성된 `GridRect` 에 대한 목적함수로** 고르도록 분리하고 Y축에 스팬 하한을 추가해 4/5 가 되었다.

**남은 실패 `4.png`:** y=151 에서 시작하는 정렬이 올바른 4행 부분격자를 택한다(행 1~4를 덮고 0·5를 버림). 3.4% 차이로 이긴다. 근본 원인은 `scoreGrid` 가 셀 점수의 **정규화 없는 합**이라, 빈칸이 18개인 `4.png` 에서는 더 촘촘한 부분격자가 총합에서 이길 수 있다는 것이다. 셀당 정규화는 오히려 악화된다(부분격자를 더 선호). 커버리지/범위 개념이 필요하다.

**중요: 폴백 경로는 행 수를 고를 수 없다(5장 전부 4행).** 스코어러 없이 `calibrateGrid` 를 쓸 경우 `rows` 를 반드시 넘겨야 한다.

### px 오차는 잘못된 성공 기준이었다

계획서가 원래 세운 "셀 중심 오차 ≤2px" 목표는 **폐기한다.** 픽스처의 `grid` 값 자체가 호스트가 같은 방법(무게중심 회귀 + 점수 스윕)으로 만든 것이므로, 그 값에 가까워지는 것은 순환 논증이다. 실제로 최종 라운드에서 px 오차는 평균 6.93px → 8~13px 로 **커졌는데 end-to-end 정확도는 올라갔다**. `3.png` 는 픽스처 격자에서 11.18px 떨어져 있으면서 정확도는 90.9% → 95.5% 로 더 높다. 즉 px 거리와 실제 성능은 이 문제에서 **역상관**이다.

### 격자 검출에서 확인된 사실

- **목적함수가 전부다.** 검출 실패는 탐색 문제가 아니라 목적함수가 오답을 더 높게 평가하는 문제였다. `1.jpeg` 는 좌측 외곽 프레임이 내부 간극보다 어두워(33.95 vs 36.0~36.4) 비용이 역전되면서 격자가 통째로 한 칸 밀렸다(65px 오차). 비용에 **셀 내부 밝기 항**(간극 밝기 − 가장 어두운 셀 내부)을 넣자 정답이 전역 최적이 되면서 1.61px 로 떨어졌다. 평균이 아니라 **최솟값**을 써야 한다 — 평균은 origin 에 따라 매끄럽게 변해 미세 평원을 기울인다.
- **모든 단계를 단조(monotone)로 만들어야 한다.** 초기 구현은 정답 격자에서 출발해도 정답을 떠났다. `4.png` 는 시드가 0.03px 로 거의 완벽했는데 후단이 10.77px 로 망가뜨렸다. 목적함수가 개선될 때만 수용하도록 바꾸는 것이 필수다.
- **죽은 단계는 목적함수가 틀렸다는 신호일 수 있다.** `refineByCentroids` 는 밝기 목적함수에서 5장 모두 기각되어 삭제 후보였으나, 매칭 스코어러로 게이트하자 5장 중 3장에서 채택된다. 삭제하지 않기를 잘했다.
- **행/열별 중앙값 축약이 필수다.** 무게중심 회귀에 평범한 최소제곱을 쓰면 잘못 분할된 셀 하나가 지렛대로 작용해 `3.png` 의 행 pitch 를 12.2px 망가뜨린다(오차 28.8px → 중앙값 축약 후 2.6px).
- **격자 검출 비용**: 이미지당 약 29회 스코어러 호출, 4.3~9.8초. 매처의 `TOPK=28`·9오프셋을 그대로 쓰면 약 30배 느려지므로 스코어러 전용 축소 경로(`TOPK=8`, 오프셋 `[0]`)가 필요하다.

**목표 미달을 분명히 기록해 둔다.** top-1 70.8%는 기준선의 5.7배지만 99%가 아니다. 원인은 두 가지다.

1. **정답 라벨이 48개뿐이다.** 이 시점부터 하이퍼파라미터를 더 만지는 것은 48개 표본에 대한 과적합이다. 실제로 스케일 추정 방식·전경 임계값·크롭 여백을 여러 조합으로 스윕했지만 66~71% 구간에서 진동했고, 이 차이는 표본 수를 고려하면 통계적으로 의미가 없다. **더 필요한 것은 파라미터 탐색이 아니라 정답 데이터다.**
2. **스케일 잠금이 이미지마다 흔들린다.** 네 PNG는 셀 피치가 119~121px로 사실상 같은데도 잠금 스케일이 42/42/34/40으로 갈렸고, 34로 잠긴 `4.png`가 가장 나빴다. 셀을 64px 캔버스로 정규화하므로 스케일은 렌더링 상수여야 한다는 가설로 전역 고정도 시도했으나 오히려 60.4%로 떨어졌다 — 즉 이미지별 적응이 스케일 외의 다른 편차까지 흡수하고 있다. 이 부분은 아직 원인이 규명되지 않았다.

### 99%까지 남은 일 (우선순위 순)

1. **정답 라벨 확대** — 현재 48개. 최소 150개는 있어야 개선 여부를 신뢰할 수 있다. `1.jpeg` 무손실 재캡처 + 신규 스크린샷 확보가 가장 효과가 크다.
2. **스케일 추정 원인 규명** — 왜 전역 고정이 이미지별 적응보다 나쁜지 아직 모른다. 아래 두 가설은 이미 시험해서 **기각**했으니 반복하지 말 것.
   - *기각 ①: 전역 고정 스케일.* 셀을 64px 캔버스로 정규화하니 아이콘 스케일은 렌더링 상수일 것이라 보고 36~46을 전역 고정해 봤다. 최고 60.4%로, 이미지별 적응(70.8%)보다 나빴다.
   - *기각 ②: 전체 캔버스 정규화.* 확정 라벨의 스프라이트를 재보니 알파 bbox 크기가 **47~70px로 제각각**이었다(80×80/100×100 캔버스 안에서). 타이트 크롭 후 정사각 정규화가 아이콘의 상대 크기 정보를 지운다고 보고, 원본 캔버스 전체를 고정 배율로 렌더하는 방식을 시험했다. 최고 58.3%(FORCE=58)로 역시 더 나빴다. 즉 게임이 스프라이트 캔버스를 균일 배율로 그리는 것은 아니거나, 위키 스프라이트의 캔버스 프레이밍이 일정하지 않다.
   → 다음 시도는 추측이 아니라 **한 장에서 아이콘 렌더 크기를 원본 픽셀로 직접 실측**해 셀 피치와의 관계를 확정하는 것이어야 한다.
3. **석판 회전 처리 재검토** — 회전 가능 석판이 많은 `5.png`가 가장 나쁘다. 회전 변형 4종이 후보 공간을 넓혀 오답을 늘리는지 확인 필요.
4. **§5 3-6 편향 보정 미구현** — 작고 어두운 템플릿 편향 정규화는 아직 들어가지 않았다.

## 9-C. 라벨 확대 후 재측정 (2026-08-09, 최신)

정답 라벨을 **48개 → 86개**로 늘렸다(미확인 70 → 32). 그 결과 측정 정확도가 **내려갔다.**

| 인식기 / 격자 | 빈칸 | top-1 | 종합 |
|---|---|---|---|
| baseline (기준선) | 57.0% | 7.0% (6/86) | 43.7% (62/142) |
| **plate-matcher + 손라벨 격자** | 97.9% | **66.3% (57/86)** | **78.2% (111/142)** |
| plate-matcher + 자동 격자 | **99.3%** | 61.6% (53/86) | 76.8% (109/142) |
| plate-matcher + 자동 격자(스코어러 없음) | 97.9% | 51.2% (44/86) | 70.4% (100/142) |

**top-1 이 70.8% → 66.3% 로 내려간 것이 이번 작업의 가장 중요한 결과다.** 48개 라벨 시절의 수치는 **낙관 편향**이었다. 라벨을 매처의 상위 후보에서 뽑아 확인하는 방식이었으므로, 매처가 애초에 찾지 못하는 아이템은 라벨이 되지 못하고 분모에서 빠져 있었다. 이번 라운드에서 카탈로그를 직접 뒤져 매처가 top-8 안에조차 넣지 못한 항목들을 라벨로 넣자 실제 성능이 드러났다. 기준선도 12.5% → 7.0% 로 같은 방향으로 움직였다.

**66.3% 를 앞으로의 기준으로 삼는다.** 회귀 방어선도 이 값으로 재설정했다(`top1Correct >= 57`, `overallCorrect >= 111`, `emptyAccuracy >= 0.975`).

### 라벨 현황

| 이미지 | 확정 | 빈칸 | 미확인 |
|---|---|---|---|
| 1.jpeg | 11 | 0 | 19 |
| 2.png | 18 | 16 | 2 |
| 3.png | 16 | 17 | 3 |
| 4.png | 16 | 18 | 2 |
| 5.png | 25 | 5 | 6 |
| **합계** | **86** | **56** | **32** |

`1.jpeg` 가 0개에서 11개로 늘어 처음으로 채점에 기여한다.

### 이번 라운드에서 확인된 사실

- **틀린 라벨 1건을 잡아 고쳤다.** `4.png` 슬롯 15 를 `swaying_eyes` 로 라벨링했었는데, 실제 셀은 초록색 V 자이고 `swaying_eyes` 스프라이트는 어두운 타원이라 전혀 다르다. 올바른 값은 석판 `exit` 이다. 1라운드 라벨이 매처 후보만 보고 판단한 결과 생긴 오류다.
- **반복 등장하는 두 석판이 라벨의 14%를 차지한다.** 초록 체크무늬 평행사변형 = `load`(7칸), 초록 V 자 = `exit`(5칸). 둘 다 매처의 top-8 에 거의 오르지 못해 1라운드에서 전부 `???` 로 남아 있었다. 카탈로그 60종을 한 장의 대조 시트로 만들어 눈으로 찾아 해결했다.
- **어두운 보라색 구체 = `black_planet`.** §10-4 에서 점유 판정이 실패하던 바로 그 아이콘이며, 3장에 걸쳐 등장한다.
- **후보 목록만 보고 라벨링하면 편향이 생긴다.** 이번에 명시적으로 카탈로그를 직접 뒤진 이유이고, 픽스처의 `_labelling` 필드에 이 사실을 기록해 두었다.

### 남은 32개 미확인 셀

대부분 `1.jpeg`(19개)이며 73px JPEG 이라 육안 식별이 어렵다. 나머지 13개는 하트, 펼친 책, 파란 물약병, 주황 적층물, 청록 보석 등 **카탈로그에서도 대응을 찾지 못한** 것들이다. 신규 아이템이거나 게임 내 렌더링이 스프라이트와 다를 가능성이 있다.

## 9-D. 사용자 제공 정답 스크린샷 투입 (최신)

사용자가 새 PNG 스크린샷 1장(`인벤 예시/스크린샷 2026-08-09 210635.png`, 6×5=30칸)과 **한글 정답 전체**를 제공했다. 픽스처 `6.png` 로 편입했다(파일명은 `imagePath` 로 참조하므로 원본 파일명을 바꾸지 않았다).

한글 이름 30개 중 28개가 `label_kor`/`ko_label` 로 즉시 해석됐고, 나머지 2개(`깃발`, `플라즈마 헬멧`)는 스프라이트는 이미 받아뒀지만 `data/*.json` 이 아직 동기화되지 않아 생긴 누락이었다. 덕분에 **신규 석판 `flag` 의 한글명이 "깃발" 임을 확인**했다(§5 1-4 의 미확인 항목 중 하나 해소).

### 결과 — 독립 정답에서 오히려 더 높다

| 인식기 / 격자 | 빈칸 | top-1 | 종합 |
|---|---|---|---|
| baseline | 55.5% | 6.9% (8/116) | 37.6% |
| **plate-matcher + 손라벨 격자** | 97.7% | **67.2% (78/116)** | 76.9% (133/173) |
| plate-matcher + 자동 격자 | 98.3% | 61.2% (71/116) | 74.0% |
| plate-matcher + 자동 격자(스코어러 없음) | 97.7% | 51.7% (60/116) | 67.6% |

**`6.png` 단독 성적: 빈칸 96.7%, top-1 72.4% (21/29), 종합 73.3%.**

이것이 이번 라운드의 핵심입니다. `6.png` 는 **후보 보조가 전혀 없는 완전 독립 정답**이고 `???` 가 하나도 없어 선택 편향이 원리적으로 없다. 그런데 여기서 top-1 이 **72.4%** 로, 자체 라벨링으로 측정한 66.3% 보다 오히려 **높게** 나왔다.

즉 **자체 라벨링이 정확도를 부풀리고 있다는 우려는 기우였다.** 오히려 보수적으로 측정하고 있었다. 실제 성능은 67~72% 구간으로 보는 것이 타당하다.

### 정답 스크린샷이 잡아낸 라벨 오류 2건

독립 정답이 있으니 기존 라벨을 교차 검증할 수 있었고, 실제로 오류가 나왔다.

- **`1.jpeg` 슬롯 9: `load` → `future`.** 초록 체크무늬 석판이 `load` 와 `future` 두 종류인데 육안으로 구분이 어려웠다. `6.png` 슬롯 10 의 정답이 `future` 라는 것을 기준점 삼아 8종 후보로 좁혀 재채점하니, 검증용 셀에서 `future=0.386 / load=0.130` 으로 3배 차이가 나 판별기 자체가 신뢰할 만함이 확인됐고, 그 판별기가 `1.jpeg#9` 를 `future` 로 지목했다. 육안 재확인 결과 회색 사각형 위치가 `load` 는 좌하단, `future` 는 우하단으로 실제로 달랐다.
- **`2.png` 슬롯 1: `???` → `heart_of_the_beast`.** 카탈로그 327종을 다 뒤지고도 못 찾았던 하트 아이콘인데, 정답의 `야수의 심장` 으로 정체가 확인됐다.

### 라벨 현황

**48 → 86 → 116개.** `6.png` 29개가 추가되고 `2.png` 미확인 1개가 해소됐다. 미확인은 31개(대부분 `1.jpeg` 19개).

### 남은 문제

- **자동 격자가 `6.png` 에서 손라벨 대비 뒤진다** (62.1% vs 72.4%). 셀 중심 오차도 14.12px 로 크다. 행 수 자동 검출도 5행을 4행으로 잘못 잡아 **4/6** 으로 내려갔다. `4.png` 와 같은 원인(부분격자가 총합에서 이김)일 가능성이 높다.
- 목표 99% 까지는 여전히 멀다. 다만 이제 **116개 라벨 위에서 측정**하므로 개선 여부를 신뢰할 수 있다.

## 9-E. 두 번째 사용자 정답 스크린샷 (`7.png`)

사용자가 두 번째 스크린샷(`인벤 예시/스크린샷2.png`, 729×728)과 정답을 제공했다. 라벨 **116 → 145개**.

### 인벤토리 마지막 행은 부분적일 수 있다

`7.png` 는 6열 6행처럼 보이지만 **마지막 행이 5칸**이라 실제 슬롯은 **35개**다(인덱스 35는 패널 바깥). 정답도 6행에 5개만 적혀 있어 일치한다. 픽스처는 `rows: 6, cols: 6, totalSlots: 35` 로 기록했다. 매처가 `i < totalSlots` 로 순회하므로 이 표현이 그대로 동작한다.

**이는 격자 검출이 행 수뿐 아니라 "마지막 행의 칸 수"까지 다뤄야 함을 뜻한다.** 현재는 처리하지 않는다.

### 정답 표기 3건 reconcile

- **`차앙` → `차양`(shade)**: 한 글자 오타. 매처가 독립적으로 `shade=0.321` 을 1위로 뽑았고 2위가 0.023 이라 14배 차이 — 두 근거가 일치해 확정했다.
- **`친타미니 돌` → `친타마니 돌`(chintamani_stone)**: 로컬 표기와 모음 하나 차이.
- **`방어수` → `defender`**: 신규 석판이라 `tablets.json` 에 `ko_label` 이 없어 미해석. 매처도 `defender=0.191`(2위 −0.027)로 지목. **신규 석판 `defender` 의 한글명이 "방어수" 임을 확인**했다(§5 1-4 해소 항목 2번째, `flag`=깃발에 이어).
- `번개 부메랑`·`고양` 은 개명된 스프라이트(`lightningboomerang`, `home-town`)로 매핑. **`data/*.json` 미동기화(Phase 1-1)가 계속 마찰을 일으키고 있다** — 우선순위를 올릴 것.

### 결과 (라벨 145개)

| 인식기 / 격자 | 빈칸 | top-1 | 종합 |
|---|---|---|---|
| baseline | 54.8% | 9.0% (13/145) | 36.5% |
| **plate-matcher + 손라벨 격자** | 96.6% | **65.5% (95/145)** | 75.0% (156/208) |
| plate-matcher + 자동 격자 | 97.6% | 61.4% (89/145) | 73.1% (152/208) |

`7.png` 단독: 빈칸 91.4%, top-1 58.6% (17/29), 종합 65.7%. `6.png`(72.4%)보다 낮다.

두 독립 정답 스크린샷의 top-1 은 각각 72.4%, 58.6% 로 편차가 크다. 표본이 29칸씩이라 이 정도 변동은 정상이며, **한 장짜리 수치를 신뢰하면 안 된다**는 근거이기도 하다.

### 격자 원점 스윕이 또 발산했다

`7.png` 원점을 매칭 점수 스윕으로 잡으려 하자 탐색 범위의 **경계값**(3.5, 3.0)을 골랐고, 육안 확인 결과 명백히 어긋났다. 내부 간극 피팅값 (8.5, 9.5)가 옳았다. Sentinel 이 지적한 edge-seeking 문제가 재현된 것이며, 스윕 목적함수에 경계 이탈 페널티가 더 필요하다는 신호다.

## 9-F. 격자 검출 완성 — 행 수 7/7, 자동 격자가 손라벨과 동률

### 결과

| 격자 | 빈칸 | top-1 | 종합 |
|---|---|---|---|
| 손라벨 (대조군) | 96.6% | 65.5% (95/145) | **75.0% (156/208)** |
| **자동 + 스코어러** | **98.1%** | 64.1% (93/145) | **75.0% (156/208)** |
| 자동, 스코어러 없음 | 96.6% | 53.1% (77/145) | 67.3% (140/208) |

**행 수 자동 검출 7/7** (이전 5/7). 사용자가 행 수를 알려주지 않는 실사용 경로가 이제 전 픽스처에서 동작한다.

### `scoreGrid` 는 정규화하지 않는 것이 옳았다

호스트는 "정규화"를 지시했지만, 측정 결과 **셀당 평균은 부분격자를 더 선호**한다(0.0457 vs 0.0294). 합(sum)이 옳은 집계자다 — 합 자체가 커버리지를 보상하기 때문이다. 문제는 집계 방식이 아니라 **부분격자라는 특정 실패 모드**였고, 그건 가중치 혼합이 아니라 **셀 단위 커버리지 규칙**으로 잡는 게 맞다.

채택한 것: `missedOutside(rect)` — 후보 격자 바로 위·아래 행에 점유 셀이 몇 개 있는지 센다. 행 수는 **사전식**으로 고른다(놓친 셀 최소 우선, 매칭 점수는 동점 처리). **조정 계수가 없다.** 신호가 선명하다.

```
정답 격자는 7장 전부 missed=0
4.png  n=4 score 1.0964 missed=1   <- 이전 승자, 이제 배제
       n=6 score 1.0600 missed=0   <- 정답
6.png  n=4 score 2.6215 missed=5   <- 이전 승자, 이제 배제
       n=5 score 2.3750 missed=0   <- 정답
```

### 진짜 개선은 다른 곳에 있었다

end-to-end 정확도(+4셀)는 커버리지가 아니라 **스윕의 도달 범위**에서 나왔다. 기하 오차가 최대 ±11px 인데 `SWEEP_DX` 는 ±4 까지만 닿아 **구조적으로 보정이 불가능**했다. 고정 5×5 스윕을 **수렴형 좌표하강**(개선이 없을 때까지 반복, 단조 유지)으로 바꾸자 해결됐다.

교훈: 목표를 못 맞출 때 목적함수부터 의심하기 전에 **탐색 반경이 정답을 포함하는지** 먼저 확인할 것.

### 기각된 것들 (과적합 회피)

- **n별 사전 정렬**: 구현해서 측정했으나 7장 × 후보 4개 중 **단 1개**의 후보만 바뀌었고(경쟁에 없던 후보) 결과는 동일. 죽은 복잡도라 제거했다. "n=6이 자기 최적점에서 평가되지 않는다"는 호스트 가설은 합리적이었으나 측정이 반증했다.
- `SWEEP_ROUNDS=2` 는 157/208 로 **1셀 더 좋았지만 채택하지 않았다.** 208개 중 1셀은 잡음이고, 그걸 고르는 것은 픽스처 튜닝이다. 라운드 4와 8이 동일 결과라 상한은 구속 조건이 아니다.
- `SCORER_SWIN=2` 는 1.67배 빨랐으나(102초→61초) 품질이 떨어져(행 5/7→4/7) 기각.

### 커버리지 프로브 보강 — 빈 마지막 행 맹점 제거

첫 구현(`missedOutside`)에는 실사용에서 자주 밟을 구멍이 있었다. **참 격자의 마지막 행이 통째로 비어 있으면** 한 행 작은 후보도 실제 아이템을 바깥에 남기지 않아 `missed=0` 으로 동점이 되고, 판정이 다시 비정규화 합으로 되돌아간다. `4.png` 가 정확히 이 경우였고 마진이 **3.0%** 에 불과했다. 인벤토리가 덜 찬 상태는 흔하므로 픽스처 우연이 아니다.

수정: 프로브 셀을 "점유됨" 뿐 아니라 **"테두리 링이 격자 자신의 셀들과 일치함"**(= 프레임이나 배경이 아니라 **빈 인벤토리 칸**)일 때도 센다. 기준자는 격자 자신의 링 편차라 **조정할 임계값이 없다.** 결과가 3% 마진에서 범주적 분리로 바뀌었다.

```
참 격자는 7장 전부 panelLike = 0
2.png  n=5 → 6   n=6(정답) → 0     (이전에는 0 vs 0, 점수로 판정)
4.png  n=5 → 6   n=6(정답) → 0     (이전 마진 3.0%)
6.png  n=3 → 6   n=4 → 6   n=5(정답) → 0
```

부수 효과로 **스코어러 없는 폴백의 행 수 검출이 3/7 → 6/7** 로 올랐다(이 프로브는 스코어러가 필요 없다). 그리고 동점이 일찍 깨져 스코어러 호출이 줄어 **스위트가 97.3초 → 92.7초로 빨라졌다.**

### 회귀 방어선을 픽스처 단위로 내렸다

집계 floor 하나로는 재분배를 못 잡는다는 것이 실제로 확인됐다 — auto 경로에서 `6.png` 가 4셀 회귀했는데 `7.png`(+5)와 `5.png`(+4)가 상쇄해 TOTAL 은 그대로였다. 이제 **픽스처별 top-1·종합 하한**을 프리즈 경로와 auto 경로 양쪽에 걸었고, 각 픽스처의 `it` 안에서 단언하므로 회귀가 **어느 스크린샷인지 이름을 대며** 실패한다.

### 남은 것

- 자동 격자가 **top-1 에서는 여전히 대조군에 뒤진다**(93 vs 95). 종합이 같은 이유는 아이템 식별 2개를 빈칸 판정 2개와 맞바꾸고 있기 때문이다.
- **`6.png` 의 auto 회귀(22/30 → 18/30)는 복구되지 않았다.** 픽스처별 floor 로 더 나빠지는 것은 막았지만 원인은 미해결이다. 기하 오차도 7장 중 최대(평균 16.39px)다.
- **폴백 경로의 행 수 검출은 6/7** (`7.png` 만 실패). 스코어러 경로는 7/7.
- `scoreGrid` 가 7장 전부에서 손라벨 정답보다 검출 격자를 높게 평가한다. **프록시가 정확도와 완전히 정렬되어 있지 않다**는 뜻이며, 격자 목적함수의 남은 개선 여지다.
- 기하 px 오차는 오히려 커졌는데(4.png 7.41→13.44px) 정확도는 올랐다. **px 거리가 잘못된 지표라는 세 번째 확인**이다.
- 스위트가 97.2초로 120초 예산에 근접했다. 스코어러 기반 캘리브레이션을 픽스처당 하나 더 추가하면 초과한다.

## 9-G. 진단 라운드 (2026-08-15) — 오답 전수 분해 + 렌더 스케일 실측

5개 병렬 조사(오답 census / 스케일 실측 / 실측 독립 검증 / 유사도구 기법 조사 / VLM 평가). census는 배포 파이프라인의 계측 사본으로 공표 수치(95/145, 빈칸 오류 7건)를 **정확히 재현**한 뒤 측정했으므로 실제 시스템에 대한 진단이다. 증거물은 `.omc/research/vision-diagnosis-2026-08-15/`에 보존(오답 50건 전부의 셀|정답|예측 3단 대조 크롭 포함).

### 발견 1 — 게임은 아이콘을 정확히 3.0 화면px/원본아트px, NN으로 렌더한다

§9-A 미해결 항목 2(스케일 추정 원인)가 **실측으로 해소**됐다.

- NN 렌더링이면 아이콘 픽셀은 s-격자 위에서만 색이 바뀐다. fg 인접 RGB 변화 위치에 위상-자유 격자 피팅을 하니 5장 PNG 30셀 중 27셀이 양축 모두 **정확히 s=3.000**, 2셀은 3과 정합하는 3/2 부격자, 반례 0.
- 픽셀 단위 재구성 성공: `flag`는 위키 스프라이트→native 복원→3× NN 업스케일이 스크린샷과 **IoU 1.000 (1908/1908px)**. 독립 검증자(7.png 29셀 + 4.png 재감사, 자체 코드)는 `vow`·`touch_of_life` **100.00% 픽셀 일치**, 13/15셀 93~100% 일치로 **confirmed** 판정.
- **스케일 락은 렌더 스케일을 잰 적이 없다.** 렌더 스케일은 상수 3인데 아이템별 원본 아트 크기가 9~25px로 제각각이라, fg 크기 P65는 "그 인벤에 뭐가 들었나" 통계였다. 42/42/34/40 드리프트의 정체. 4.png가 34로 잠긴 것은 아이템 구성 + 어두운 플레이트에서 어두운 외곽선이 FGT=45 아래로 떨어져 fg bbox가 원본 3~12px씩 줄었기 때문(디버그 크롭으로 육안 확인).
- 셀 피치(118.85~120.27px)와 s는 **비례하지 않는다** — s는 양자화된(정수) UI 스케일. pitch/s ≈ 39.6~40.1 native px/셀. 73px 피치인 1.jpeg는 s=2로 추정되나 **미검증**(JPEG 노이즈가 격자 판정을 깨서 실측 불가).
- 주의(검증자 발견): 글로우/이펙트가 있는 아이콘은 이펙트가 **화면 해상도로** 렌더되어 s-격자를 벗어난다. 격자 기반 s 추정은 아이콘 코어(높은 fg 임계)만 써야 한다.

### 발견 2 — 위키 스프라이트는 스프라이트별 유리수 k의 NN 업스케일

- 정수 k-블록 재구성·run-length GCD는 전부 실패(k가 비정수 + 위상 임의). 성공한 방법: 인접 동일-알파 행/열의 변화 위치를 c_i = o + m_i·p로 그리디 정수 배정 + 최소제곱 피팅, 잔차 스팬 < 1.0 양축 수용.
- 측정된 k: 80px 캔버스 아티팩트 대부분 **정확히 2.5**(검증자 재현 17종), 100px 석판 **정확히 3.0**(warrant/vow/shade/home-town), **~2.45 계열**(ruby_brooch, sheet_music_storm, flux_mk2, frozen_bow, solis_decree, lightning_chakram 등 — 2.5의 ~98% 재익스포트 추정, 중간 위상 슬립 있음), 저신뢰(defender 2.6, flag 2.73), **추출 불가**(스무스 리샘플: heart_of_the_beast, storm, amulet_of_power, lightningboomerang).
- 교차 검증: spriteBbox/k == 스크린샷 실측 native(renderedBbox/3)가 깨끗한 측정 전 셀에서 ~1 native px 이내 일치 (ruby 20×18 vs 20.0×18.3, black_scales 25×17 vs 25.2×17.1 등 10+셀).
- **alpha bbox 정규화(toSquare)가 k와 native 크기를 뒤섞는 것**이 §9-A 기각 실험들(전역 고정 스케일, 전체 캔버스 정규화)이 실패한 이유다.

### 발견 3 — 오답 50개(라벨 145 중) 원인 분해

| 원인 | 건수 | 비고 |
|---|---|---|
| stage-1 프리필터 탈락 | **31** | 그중 **22건은 프리필터 우회 시 stage-2가 정답 1위**(26건은 top-10). 공통 시그니처: 아이콘이 작게 렌더(fg bbox 18~33px)돼 락 윈도우 스케일의 마스크와 IoU 붕괴 |
| 스케일 윈도우 제외 | 8 | 8건 전부 정답 최적 스케일이 윈도우 **아래**(26~28). P65 락은 작고 어두운 아이콘에서 위로 편향 |
| 라벨 오류 의심 | 8 | 전부 자체 라벨 픽스처(3.png·5.png), 전부 유사 계열. 사용자 정답 픽스처(6/7.png)에선 0건 |
| 카탈로그 불일치 | 5 | 아래 참조 |
| 점유판정 false-empty | 5 | 어두운 구체·~15px 초소형(MIN_BLOB_RATIO 미달)·kunai(링 오염 가설)·저대비 |
| 어두운-작은 편향 | 1 | 주원인으론 거의 소멸. 단 정답 부재 시 기본 승자(ohia_lehua×4, black_scales×4, honor×4…) — 저신뢰 신호로 활용 가치 |
| 희소 아이콘 | 1 | 1px 스트로크 활 + 오버레이 오염 |
| **JPEG / 회전 / 유사아이템** | **0** | 1.jpeg 오답도 전부 프리필터/스케일 메커니즘. stage-2는 load/future를 3배 마진으로, 배지 계열도 깨끗이 분리 |

- 계측 실측: stage-2 전수 매칭(429 변형)은 Node에서 셀당 ~0.3s(7픽스처 62s). **프리필터가 밥값을 못 한다.**
- 라벨 오류 의심 10건(아이템 8 + 빈칸 2: 3.png#10, 5.png#25): 5.png#2 wit→preparation, #3 unity→distribution, #4 hope→advance, #13 hope→connection, #17 keel_fragment→kaleidoscope, 3.png#0 →swordsmanship_textbook, #7 warrant→honor, #16 thornbush→thorn. **수정은 크롭 육안 확인(사용자) 후에만** — census의 시각 판정이지 정답 제공이 아니다. 확정 시 실제 top-1은 ~71%였던 셈.

### 카탈로그 불일치 (로드맵 4 대상)

- **`defender` 위키 스프라이트는 인게임 아이콘과 완전히 다르다** (3장 교차 확인; 인게임 아이콘은 오히려 `dedication` 스프라이트와 흡사 — 매처가 매번 dedication을 뽑는 이유). 정답은 전 스케일에서 음수 점수.
- **`keel_fragment`도 아트 불일치**(검증자 발견): 위키는 60×45 판자, 인게임은 ~16×15 native 곡면 껍질. 재구성 0.202 vs 정상 0.93+.
- **소진 상태(`-1/2` 오버레이) 아이템은 어두운 실루엣으로 렌더**된다(eternal_winter, fascinating_lure). 밝은 위키 스프라이트로는 원리적으로 매칭 불가 — 어둡게 변형한 템플릿 또는 상태 감지 필요.

### 기법 조사·VLM 평가 결론

- ~99~100% 실용 정확도에 도달한 유사 도구(MAA, arkntools 브라우저 창고 인식, penguin-stats)는 전부 **픽셀 충실한 게임 기준 템플릿 + 고전적 픽셀 매칭**. 아이콘 판별에 CNN을 쓴 곳은 없다. CNN/임베딩(합성 데이터 학습 + ONNX Web)은 위 충실도 작업 후에도 JPEG/리사이즈가 목표 미달일 때의 Phase B.
- **VLM 폴백은 SKIP**: 저신뢰 셀(리컬러 등 fine-grained)이 VLM 최약 영역이고, Phase 4 확인 UI가 같은 셀을 100%로 만든다. 재평가 게이트: 확인 UI 출시 후 개입률 >~10% 지속 + 자체 평가에서 VLM이 매처 top-1을 유의하게 상회할 때만, 확인 UI 프리필로만.

### 로드맵 (효과 순 · 이번 라운드는 1·2 구현)

1. **프리필터 제거/무손실화** — 예상 +22셀 (65.5%→~81%)
2. **스프라이트별 결정론적 스케일** — 오프라인 k 카탈로그(327종) + s=round(pitch/40) 가설, 스프라이트당 단일 스케일(±슬랙). 예상 +8셀 (→~86%), 후보 공간 ~4배 축소
3. 라벨 10건 재검증(사용자 확인 필요)
4. 카탈로그 수리(defender·keel_fragment 실렌더 템플릿, 소진 상태)
5. 점유 판정 보강(+5셀) — 스케일이 결정론화되면 fg bbox가 스케일 정보를 짊어질 필요가 없어짐
6. PNG + 정수 스케일 확인 시 **원본 좌표계 픽셀-정확 매칭** — 99%+ 경로

### 로드맵 1·2 구현 결과 (2026-08-15, 같은 날)

| 지표 (손라벨 격자) | 이전 | 이후 |
|---|---|---|
| 아이템 top-1 | 95/145 (65.5%) | **119/145 (82.1%)** |
| 종합 | 156/208 (75.0%) | **181/208 (87.0%)** |
| 빈칸 | 96.6% | 96.2% (200/208) |

| 자동 격자 | 이전 | 이후 |
|---|---|---|
| top-1 / 종합 | 93 / 156 | **126 / 188 — 손라벨(181)을 상회** |
| 행 수 검출 | 스코어러 7/7, 폴백 6/7 | **양쪽 7/7** |
| 폴백 종합 | 140 | 168 |

census가 지목한 플립 예상 14셀 중 **12셀 플립 성공**. 미플립 2건: `1.jpeg#28 exit`(lowConfidence 스프라이트가 s=2 JPEG에서 entrance에 밀림), `2.png#1 heart_of_the_beast`(스무스 리샘플 위키 스프라이트가 인게임 렌더를 재현 못함 — defender/keel과 같은 **카탈로그 수리 계열**, 로드맵 4). 회귀 1건: `1.jpeg#16 six_leaf_clover`가 빈칸으로(lowConf k 추정 + s=2 외삽 창 밖). **1.jpeg의 s=2 외삽은 성립** — top-1 6→9, load×3·future 전부 플립.

**k 카탈로그** (`data/sprite-natives.json`, `npm run build:sprite-natives`): 327종 = lattice 253 / fallback 72 / pinned 2. 검증 앵커 14/14 통과, §9-G의 defender 2.60·flag 2.74 저신뢰값 정확 재현. 새 사실: **석판 60종 중 100px 캔버스는 22종뿐**(그것만 k=3.0; 나머지 38종은 80px에 k≈2.5) — §9-G 본문 표현의 정정. 재구현 시 함정: 변화 위치가 정수 반올림이라 **k·2/3 하모닉(2.5→1.667)이 잔차가 더 작게 피팅**되므로 피치 창 [1.8, 4.01]을 최소제곱 후에도 강제해야 한다. fallback 72 중 23은 손실 webp, 49는 실측상 off-lattice(스무스 리샘플 계열).

구현 결정(전부 하네스 실측으로 판정):
- **프리필터 완전 제거.** 스프라이트별 정확한 스케일에서 IoU를 재측정해도 TOPK=96에서 정답 40/140 손실 — 어두운 아이콘의 fg 부족 추출이 IoU 순위를 구조적으로 붕괴시킨다. 스코어러 경로의 TOPK=8 깔때기도 2.png에서 참 격자를 음수로 만들어 제거. **양 경로 모두 429 변형 전수 채점.** stage-2 전수는 셀당 ~0.15s.
- 스케일: `S_SCREEN = clamp(round(pitchX/40),1,8)`, `scale_j = clamp(round(S_SCREEN·nativeSide_j·64/span),8,62)`. 슬랙 {-2..+2}(실측: {-1,0,1}·{-2,0,2}보다 2셀+ 우위), lowConfidence 스프라이트는 [-8..+2](k 추정 오차가 하방 비대칭; 이것만이 2.png 플로어 17을 지키는 경로).
- **score≤0 → 빈칸** 규칙(recognize 전용): 합성이 맨 플레이트보다 셀을 더 잘 설명하지 못하면 빈칸. 경계가 0으로 구성적이라 튜닝 상수 없음. 프리필터 제거 후 정크 블롭 억제에 필수, top-1 비용 0.
- 스코어러 정직화: recenter 제거(재중심화가 어긋난 격자의 "빌린 아이콘 조각"을 세탁 — 5.png에서 5px 어긋난 격자가 참을 이겼음), 정사각 피치 가드 [0.95,1.05](실측 0.978~1.021; 행수 앨리어스는 정확히 0.90에 위치), (image,rect) 메모.
- `grid-calibrate.ts` 한 지점 수정: 이미지 경계에 클리핑된 커버리지 프로브 셀을 과반 가시 시 검사(7.png처럼 originY≈0인 격자의 부분격자 맹점 — §9-F 프로브가 잡으라던 바로 그 구멍). 부수 효과로 폴백 행 검출 6/7→7/7.
- 성능: (variant,scale) 지연 캐시 + opaque/blended 픽셀 분리 + invalid-prefix 이분 탐색으로 192s→**139.6s** (예측 비트 동일). 예산 여유는 거의 소진 — 다음 스코어러 추가 전 최적화 필요.
- 기각(재시도 금지): offset(0,0) 스크리닝(전수 승자가 317위까지 분포 — 오프셋이 희소 아이콘에 필수), raw-gain 스코어러 집계(어긋난 격자가 더 큰 총 편차를 설명: 765k vs 664k), recenter+드리프트 제외 조합 2종.

잔여 top-1 오답 26개는 census 예측대로 분해된다: **20 기지**(라벨 의심 8, defender×3, keel×2, 소진상태 2, 점유 false-empty 5, 희소 1) + 어려운 케이스 6(위 미플립·회귀 포함; `5.png#1 flag`·`#21 defect_probe`는 아트/렌더 불일치 계열). 참고: `3.png#7`은 이제 honor를 예측하므로 라벨 의심이 확정되면 공짜로 +1.

독립 리뷰 승인(전 수치 재실행 일치, 픽스처 무결, 플로어 raise-only 확인). 플로어는 손라벨 {0.9615, 119, 181} + 픽스처별, 자동 {126, 188, 168} + 픽스처별로 재고정.

### 라벨 라운드 3 — 의심 10건 전량 사용자 확정 (2026-08-15)

§9-G census의 라벨 의심 10건을 셀|현재|제안 3단 대조 크롭 시트로 사용자가 직접 검토, **10건 전부 오류로 확정**했다. 아이템 재라벨 8건(wit→preparation, unity→distribution, hope→advance, hope→connection, keel_fragment→kaleidoscope, shield_technique_manual→swordsmanship_textbook, warrant→honor, thornbush→thorn — 마지막은 ARTIFACT→TABLET 타입 정정 포함), 빈칸 정정 2건(3.png#10→swaying_eyes, 5.png#25→**red_dew** — "붉은 이슬"로 사용자가 정체 확인; "0/3" 오버레이의 소진 상태 렌더로 추정). 아이템 라벨 145→**147**개.

| 지표 | 라벨 정정 전 | 정정 후 |
|---|---|---|
| top-1 (손라벨) | 119/145 (82.1%) | **128/147 (87.1%)** |
| 종합 (손라벨) | 181/208 (87.0%) | **189/208 (90.9%)** |
| top-1 (자동 격자) | 126/145 | **135/147 (91.8%)** |
| 종합 (자동 격자) | 188/208 | **196/208 (94.2%)** — 빈칸 99.0% |
| 3.png | 80%대 | **전 항목 100%** |

재라벨 9건이 즉시 정답 전환(매처는 이미 전부 맞히고 있었다 — 자를 고친 것). 남은 것은 red_dew(#25) 1건뿐이며 이는 소진 상태 + 손실 webp 스프라이트로 **로드맵 4 대상**이다.

잔여 오답(손라벨 19 / 자동 12)은 이제 전부 원인 기지: **아트/카탈로그 불일치 계열 10** (defender×3, keel_fragment, heart_of_the_beast, flag, defect_probe, eternal_winter·fascinating_lure 소진 상태, red_dew), **점유 판정 7**(black_planet, amulet_of_power, swaying_eyes, kunai, colorless_cube, balisong·six_leaf_clover 억제), **희소 아이콘 1**(frozen_bow), **JPEG 저신뢰 1**(exit). 즉 로드맵 4(카탈로그 수리)와 5(점유 판정)가 남은 오답의 ~90%를 겨냥한다.

라벨 정정은 채점 자 자체의 교정이므로 플로어는 raise-only 원칙의 예외 없이 전부 상향 재고정됐다: 손라벨 {0.9615, 128, 189}, 자동 {135, 196, 174} + 픽스처별.

### 로드맵 4 구현 결과 — 실렌더 템플릿 수확 (2026-08-16)

확정 라벨 셀에서 원본 해상도 크롭을 3px 격자 정합 다운샘플로 native 아트로 역산해(`scripts/harvest-rendered-sprites.ts`, `npm run harvest:rendered`) 아트 불일치·소진상태 스프라이트를 수리했다. 산출물: `public/images/rendered/*.png` 7장 + `data/rendered-overrides.json`(replace/extra/extra-depleted 3종 kind; 프로덕션 로더도 동일 적용 필요). 엔진(`lib/vision/*`)은 무변경 — 같은 value의 TemplateSource 복수 등록만으로 동작.

| 지표 | 이전 | 이후 |
|---|---|---|
| top-1 (손라벨) | 128/147 (87.1%) | **133/147 (90.5%)** |
| 종합 (손라벨) | 189/208 | **194/208 (93.3%)** |
| top-1 (자동 격자) | 135/147 | **140/147 (95.2%)** |
| 종합 (자동 격자) | 196/208 | **201/208 (96.6%)** |

해결: defect_probe, keel_fragment(낮은 임계 fgt=32 + 외곽선 복구 재수확으로 89→175픽셀), eternal_winter·fascinating_lure(소진 변형), defender 1/3셀, heart_of_the_beast(자동 격자에서 교차검증 플립 — 6.png 수확본이 2.png를 맞춤).

이번 라운드에서 확정된 사실들:
- **defender는 인게임 렌더가 2종이다.** 7.png#22는 위키 아트(녹색 체크 평행사변형)와 일치, 2/4/5.png는 네이비 다이아/X(녹색 팁+빨간 중심). §9-G의 "위키가 틀렸다"는 결론은 불완전했다 — 수확본을 추가 변형으로, 위키를 폴백으로 공존시켰다(교체했으면 7.png#22 회귀). 리뷰어가 기계적으로 확증(위키는 2/4/5 셀에서 전부 ≤0점).
- **소진 상태 렌더 변환은 균일 RGB 곱셈이 아니다**(채널 의존·고분산 실측) → 카탈로그 전체 합성 불가, 셀 수확으로만 처리. 추후 안정 변환 발견 시 합성 경로.
- **오버레이 글리프 오염 함정**: 1차 수확본 3장에 카운터 글리프("0/6"·"-1/2")가 구워져 들어가 자기 셀 매칭을 부풀렸다 — 교차 계열 리뷰(grok 구현 ↔ Claude 검증)가 픽셀 수준 검사로 적발, 재수확으로 제거. 세정 후 eternal_winter의 **자동 격자 플립이 풀렸고**(가짜 플립이었음) 해당 플로어만 사유 주석과 함께 하향(측정 기구 교정 — 라벨 라운드와 동일 원칙). 나머지 플립은 세정 후에도 유지 = 진짜.
- **자기참조 한계 명시**: 수확 소스 셀 7곳의 플립은 구성상 약한 증거다(overrides JSON에 셀별 기록). 진짜 교차검증 성공은 heart(6.png→2.png, 자동)와 exit(6.png 수확, 타 PNG 3셀 유지) 뿐.
- defender 잔여 2셀(4.png#25, 5.png#5)의 미플립은 스케일이 아니라 **전이 마진** 문제(학습 플레이트가 네이비 본체를 FGT 아래로 흡수 + 스파클/'+2' 뱃지 잡음). flag(5.png#1)·red_dew(5.png#25 — 점유가 오버레이 글리프를 blob으로 잡음)와 함께 **로드맵 5 소관**.
- 스위트 벽시계 147~151s — 150s 예산 포화(런 분산 수준, 회귀 아님). **다음 라운드 전 성능 작업 필수.**

잔여 top-1 오답 14개(손라벨): 점유 판정 계열 8, defender 전이 2, flag fg 1, heart 손라벨 1, exit JPEG 1, frozen_bow 희소 1 — 로드맵 5(점유·잡음 마스킹)가 대부분을 겨냥한다.

실행 기록: 구현 = Cursor 게이트웨이 grok-4.6(high) — fable-5-1m 라우트가 502 failed_precondition(usage-based pricing 미활성 추정)으로 불통이라 사용자 승인 하에 스필. 검증 = 호스트 세션 Claude 서브에이전트(E1: verify 좌석, 대상과 교차 계열). 리뷰 1회전 fix-required(blocker 2) → 수정 → 재검증 approve.

### 성능 라운드 (2026-08-16) — 스위트 150.9s → 93.1s

행동 무변경(비트 동일, 사전 캡처 리포트와 diff 불일치 0 증명) 순수 최적화: 불투명 픽셀 SAD 정수 패킹(Uint32 r|g<<8|b<<16 + 4-wide 언롤; argmin-delta ≡ argmax-score 등가 변환, 타이브레이크 동일), prepare()/scoreGrid 메모, grid.test.ts의 인식기-스코어러 인스턴스 공유, 템플릿 병렬 디코드(pool 16), nearestIndexTable 캐시. 장대는 grid.test.ts(픽스처당 5회 인식 + 스코어러 ~29회)였다. 이후 라운드의 반복 측정 비용을 확보하기 위한 선행 작업.

### 로드맵 5 구현 결과 — 점유 판정 재작업 + 잡음 마스킹 (2026-08-16)

| 지표 | 이전 | 이후 |
|---|---|---|
| top-1 (손라벨) | 133/147 (90.5%) | **141/147 (95.9%)** |
| 종합 (손라벨) | 194/208 | **202/208 (97.1%)** |
| **빈칸 (손라벨)** | 200/208 (96.2%) | **207/208 (99.5%)** |
| 자동 격자 | 140/201 | 140/201 (유지 — 자동 크롭은 해당 셀들을 이미 다르게 처리하고 있었음) |
| 폴백 종합 | 176 | **184** |

플립 8셀: black_planet(5.png#30), amulet_of_power(6.png#0), swaying_eyes(7.png#7), kunai(7.png#26), colorless_cube(7.png#30), red_dew(5.png#25), balisong(5.png#26), defender(5.png#5). §10-4의 점유 판정 실패 계열이 사실상 소멸했다(기지 false-empty 7→1). 회귀 0, 61개 빈칸 셀 신규 오점유 0.

**구현 구조** (엔진: `lib/vision/plate-matcher.ts`):
1. **잡음 마스크 (양측 대칭, §10-2 준수)** — 흰/빨간 극단색 시드의 연결 성분 중 **TOPCUT 띠에서 기원하는**(minY≤TOPCUT+2) 것만 제거: 소형 스파클(면적≤12), 좌측 카운터(maxX≤22·면적≤80), 우측 뱃지(minX≥44). 팽창은 1픽셀만(3픽셀은 frozen_bow의 얼음을 먹어 7.png#4를 뒤집음). 마스크는 fg 추출·무게중심·D 기준선·합성 delta **양쪽에서 동일하게** 제외(코드 추적으로 대칭성 검증됨). 보조 게이트 상수(리뷰 지적으로 기록): 외곽선 비율 0.25, 어두운 이웃 <90, h≤12, w≥4, 종횡비≤4, compact 0.5(스파클)/0.3(카운터).
2. **2패스 점유 판정** — 1패스는 현행(+마스크). 플레이트 학습 후, 불확실 셀만 재분석: (a) 빈칸 판정인데 점유 플레이트 잔차가 더 가까운 셀(슬랙 2, [0,6] 스윕 무감), (b) 오버레이-그랩(cy<20)/링 오염(ring≥24·n<200) 셀. FGT2=32·MIN_BLOB_RATIO2=0.01로 **학습된 플레이트 링 우선**(자기 오염 해소 — kunai), 자기 링 차선(초소형 어두운 아이콘). 수용은 pre-grow CC 기하 게이트: 중심 내부, 링 비율<50%, ccSize≤48, ccCompact≥0.28. **2패스는 recognize 전용** — scoreGrid에 넣으면 잘못된 격자 위에서 정크를 점유시켜 자동 격자가 무너진다(측정으로 확인). analyzeCell 기본 경로는 비마스크 유지(격자 캘리브레이션의 무게중심 회귀·커버리지 프로브 고정).
3. score≤0 빈칸 규칙 유지.

**과적합 검증(교차 계열 리뷰)**: 하중 상수들을 ±20% 스윕 — 위험 방향(정크 허용)은 전부 플래토(61 빈칸 오점유 0, 최악 상황인 plateEmpty=null 강제 열화에서도 비트 동일). 얇은 마진은 회복 방향에만 존재(ccCompact 0.28 vs kunai 0.29 등 3건) — 실패 시 로드맵5 이전 동작으로 퇴화할 뿐 정크를 넣지 않는 단방향 구조. "±20%에서 다수 셀 변동" 기준으로 픽스처 튜닝 아님 판정.

**잔여 오답 6개(손라벨)**: 1.jpeg#16(점유 복구됐으나 lowConf 스프라이트가 S=2 JPEG에서 score≤0 — exit #28과 함께 **JPEG 계열 2**), 2.png#1 heart(손라벨만, 자동 격자는 정답), 4.png#23 frozen_bow(1px 스트로크 정체성), 4.png#25 defender(전이 마진), 5.png#1 flag(어두운 플레이트 fg 부족). 자동 격자 기준 잔여 7개. **로드맵 6(원본 좌표계 픽셀-정확 매칭)이 남은 마지막 단계**이며, JPEG 계열은 무손실 재캡처 권장이 현실적 해법이다.

실행 기록: 성능·로드맵5 구현 = Cursor grok-4.6(high), 검증 = 호스트 세션 Claude 서브에이전트(E1, 교차 계열; 임시 수정 파일은 해시 검증 원상복구 확인). 리뷰 approve(마이너 3 — 보호층 오귀속 주석은 호스트가 정정, 보조 상수는 본 기록에 반영).

### 로드맵 6 구현 결과 — 원본 좌표계 픽셀-정확 재순위 (2026-08-16)

| 지표 | 이전 | 이후 |
|---|---|---|
| top-1 (손라벨) | 141/147 (95.9%) | **144/147 (98.0%)** |
| 종합 (손라벨) | 202/208 | **205/208 (98.6%)** |
| 자동 격자 top-1/종합 | 140 / 201 | **142 / 203** |
| 폴백 종합 | 184 | **195** |

`lib/vision/exact-native.ts`(신규) + recognize() 후단 재순위 패스. 무손실(PNG) + 정수 S_SCREEN일 때만 발동: lattice/수확/핀 스프라이트의 **native 아트 뱅크**(위키 스프라이트를 k·위상 역산 NN 다운샘플, 왕복 일치 ≥0.85 요구 — 미달 스프라이트는 면제) → stage-2 상위 8 + 동일 value 형제 + (stage-2가 약할 때) 전 변형 시드 스캔으로 풀 구성 → 원본 좌표계 coarse-to-fine 배치 탐색(step-4 → ±4 정제; 무게중심 ±4만으로는 frozen_bow가 12px 벗어나 실패) → **정확 일치율 ≥0.9 + 격차 ≥0.15**일 때만 오버라이드.

플립 3셀(전수 감사로 오버라이드가 정확히 이 3건뿐임을 리뷰어가 독립 계측 확인): `2.png#1 heart` exact **1.000** vs 차점 0.488 · `4.png#23 frozen_bow` **0.965** vs 0.564 · `4.png#25 defender` **1.000** vs 0.160. 회귀 0. JPEG 픽스처는 바이트 동일(lossless=false 경로).

- **`flag` 아트 불일치 확진**: 위키 flag(k=2.74 저신뢰)는 전 셀 탐색에서도 exact ~0.05 — 인게임 렌더가 위키 NN 업스케일이 아니다. `5.png#1`은 카탈로그 수리 대상(6.png#6에서 수확하면 5.png#1 플립이 **진짜 교차검증**이 됨 — 로드맵 4 후속).
- **BAR/GAP 안전 마진 정정(리뷰 major)**: 구현 보고의 "BAR∈[0.57,0.96] 동일 결과" 주장은 과장 — 실측상 **BAR=0.57에서 5.png#1이 entrance→exit로 오오버라이드**되고 GAP=0.00에서는 정확도 0 동점들이 스퓨리어스 발화한다. 검증된 안전 범위는 **운영점 0.9/0.15의 ±0.05**뿐이다. 이 값들을 완화하지 말 것.
- CellPrediction에 `candidates`(top-3) 추가 — 동작 중립, 확인 UI 소비용.

### Phase 5 구현 결과 — 프로덕션 UI 이식 + 확인 UI + 레거시 제거 (2026-08-16)

§5 5-1/5-2와 Phase 4 4-1/4-2가 완료됐다. **§9-G 엔진이 처음으로 실제 서비스 경로에 연결됐다.**

- **새 파이프라인**: `lib/vision/template-catalog.ts`(순수 조립 모듈 — 하네스와 브라우저 공유, 하네스 리팩터는 비트 동일 증명) → `browser-loader.ts`/`browser-decode.ts`(워커 내 fetch로 327 스프라이트+카탈로그 로드, sprite-natives 목록 기준이라 data/*.json 지연과 무관) → `workers/recognition.worker.ts`(OpenCV 없음; calibrateGrid cols:6 + 스코어러, rows 힌트 없는 프로덕션 호출) → `ScreenshotUploader.tsx`(드롭/클릭/Ctrl+V, PNG면 lossless=true) → 스토어 배치.
- **확인 UI**: 저신뢰 점유 셀(score<0.25 또는 top1-top2 격차<0.05)에 노란 "확인" 배지; 셀 클릭 → top-3 후보 썸네일+한글명+점수 + "비우기" + 전체 카탈로그 검색(한/영/value) 피커; 선택이 슬롯을 덮어쓰고 배지 해제.
- **레거시 제거**: templateMatcher·gridDetector·vision.worker·useVisionWorker·useRecognition·SmartUploader·ImageUploader·public/opencv 삭제, `@techstark/opencv-js` 의존 제거. 인식 경로 단일화.
- 게이트: type-check·`next build`(4/4 정적 페이지)·dev 스모크(GET / 200) 통과. 스위트 무영향(엔진 트랙 상승분만 반영).
- **알려진 캐비앗**(수동 QA 목록에 포함): ① 프로덕션 배치가 rows×cols라 부분 마지막 행(35슬롯) 처리 미구현 ② `data/*.json`이 카탈로그보다 뒤처짐(아티팩트 26·석판 6종 미등재 — **Phase 1-1 동기화가 다시 마찰점**, 매칭은 되나 배치 실패 가능) ③ 업로드→피커까지의 실사용 흐름은 사람 육안 QA 필요.

통합 검증: 두 트랙 변경 파일 교집합 0, candidates/lossless 계약이 엔진→워커→업로더→스토어→피커까지 배선 확인, 스위트 24/24(144/205), 빌드·스모크 통과.

실행 기록: 스테이지드 워크플로 5런 — 구현·통합 = cursor grok-4.6(high), 트랙별 리뷰 = sonnet(E1, 교차 계열). R6 리뷰 major 1(스윕 마진 과장 — 본 기록으로 흡수), P5 리뷰 지적 0.

**남은 미도달 (손라벨 3셀)**: `1.jpeg#16`·`#28`(JPEG 73px — 무손실 재캡처가 해법), `5.png#1 flag`(위 카탈로그 수리로 해소 가능). 여기서부터는 확인 UI가 실질 100%를 담당한다(§7 설계 그대로).

### 원격 병행 라인 병합 (2026-08-16)

git 연결 시 원격(`origin/main`, 8/15 스냅샷)에 이 트리와 별개로 진행된 병행 라인(`PLAN/07~09`, `/workspace/sephiria`)이 발견되어 유효성 검토 후 선별 병합했다.

- **병합**: 옵티마이저 점수 실버그 수정 일체(PLAN/07-A·E — 공유 `lib/optimizerScore.ts`, 휴리스틱 제거, OOB 기하 재사용, SA 8초, lastOptimize 표시, **인식 아티팩트 레벨 0→카탈로그 기본값 보정**) + 새 캡처 정답 5세트(live-6~9·new1, 독립 검증 자산) + `lib/vision/last-row.ts`(부분 마지막 행 — 프로덕션 연결은 후속) + `wedge.png`(카탈로그 328번째, k=2.85 저신뢰) + **data 카탈로그를 원격 동기화본으로 갱신(아티팩트 241→267, 석판 54→61 — 카탈로그 지연 캐비앗 부분 해소)**. 충돌 3파일(store·page·types)은 Phase 5 버전에 그래프트만 접목.
- **폐기**: 구 스택 위 인식 개선(RecognitionReview·SmartUploader 개편 — Phase 5가 대체), 히스토그램/OpenCV 잔재, confusable-rerank. **임베딩 실험(PLAN/09)은 문서·스크립트만 보존** — 그 라인 자신의 실측 결론이 "embed 32/45 < plate-matcher 44/45, 하이브리드는 퇴행, plate-matcher 유지"였다.
- 게이트: 스위트 24/24(템플릿 331, 플로어 전부 유지) + 옵티마이저 테스트 25개 + type-check + `next build` 통과 후 커밋.
- 후속 후보: live-6~9 정답으로 현행 98% 엔진 독립 채점, last-row의 프로덕션 워커 연결, PLAN/07 E-7(다른 환경에서의 격자 핀 편차 — sharp/libvips 빌드 차이 가설) 확인.

### 캐노니컬 선언

이 라운드부터 **`lib/vision/plate-matcher.ts`(TS)가 캐노니컬 구현**이다. `tests/vision/REFERENCE.py`는 교체 전 알고리즘의 역사적 레퍼런스로 동결하며 더 이상 동기화하지 않는다. 판정 기준은 레퍼런스 재현이 아니라 픽스처 하네스다.

## 9-B. 보류한 저위험 결함 (Sentinel 리뷰, 미수정)

이번 웨이브에서 의도적으로 다루지 않았다. 전부 현재는 미발현이거나 완전 동점 상황에서만 영향이 있다.

- **동점 시 후보 선택 순서가 레퍼런스와 다르다.** 레퍼런스는 `np.argpartition`(동점 순서 미정의) + Python set 순회, TS는 전체 정렬. 추가로 `harness.ts` 의 `readdirSync` 는 정렬을 보장하지 않아 variant 인덱스 순서가 플랫폼 의존이다. 점수가 완전히 같을 때만 갈린다.
- **`newreg` 누산 정밀도.** numpy는 float32, TS는 float64. 상대오차 ~1e-7로 근소 동점을 뒤집을 수 있다.
- **`sharp` 출력 채널·깊이 미검증.** `.ensureAlpha().raw()` 가 RGBA8 이라고 가정한다. 그레이스케일 PNG는 2채널, 16bit PNG는 채널당 2바이트를 반환하므로 그런 자산이 하나만 섞여도 전 템플릿이 조용히 깨진다. 현재 327개 전부 RGBA8 이라 미발현.
- **`ringScratch`/`ringSeen` 이 모듈 전역 가변 상태**라 `analyzeCell` 이 재진입 불가하다. 두 recognizer 를 동시 실행하면 오염된다. 현재 핫패스에 `await` 가 없어 안전.

## 10. Phase 0에서 새로 확인된 사실

계획 수립 시점에는 몰랐고, 실제로 구현·측정하면서 드러난 것들이다. Phase 2·3 설계에 직접 반영해야 한다.

### 10-1. 격자 자동 검출은 생각보다 어렵다 — 세 방법 모두 실패

- **에지 에너지 투영**: 바깥 프레임을 셀 경계로 오인. `2.png`에서 `originY`를 실제보다 11px 위로 잡았다.
- **어두운 간극(gap) 피팅**: 격자 맨 바깥 선에는 대응하는 간극이 없어 편향된다. 내부 선만 쓰도록 고쳐도 `1.jpeg`에서 발산했다.
- **색 기반 연결요소**: 셀 내부(V≈73)와 프레임(V≈116), 빈칸(V≈98)의 명도가 너무 가까워 분리 불가.

**성공한 방법(Phase 2에 채택할 것):** 전경 무게중심 회귀 + 매칭 점수 스윕의 2단계 자기 캘리브레이션.
셀별 전경 무게중심의 셀 중심 대비 편차를 행·열 인덱스에 회귀시키면 기울기가 pitch 오차, 절편이 origin 오차가 된다. 이걸 3회 반복해 pitch를 잡고, 남은 상수 편차는 실제 매칭 점수를 최대화하는 origin 스윕으로 제거한다. 5장 전부에서 육안 검증을 통과했다.

### 10-2. 매칭 점수 설계에서 반드시 피해야 할 함정 세 가지

실제로 겪은 실패다. 재현 가능하다.

1. **단일 스케일 coarse prefilter 금지.** 축소 해상도 1스케일로 상위 후보를 거르자 정답이 그 단계에서 탈락해 정확도가 무너졌다(`single_tree_charcoal`이 36칸 중 20칸에서 1위). 복수 스케일에서 넉넉히 남겨야 한다.
2. **UI 텍스트 영역은 양쪽 마스크에 동일하게 적용할 것.** 셀 전경에서만 상단 텍스트 띠를 잘라내고 템플릿 마스크에는 적용하지 않으면, 위로 긴 아이콘이 구조적으로 불리해진다. 유효 영역(VALID)을 정의해 **셀과 템플릿 양쪽에서 동일하게** 제외해야 한다. 이걸 고치기 전에는 정답이 대부분 2~4위에 머물렀다.
3. **배경은 단일 색이 아니라 다색 모델이어야 한다.** 석판 칸 배경에는 마름모 패턴이 있어 중앙값 한 색으로는 패턴이 전경에 남는다. 테두리 링 색을 양자화해 4% 이상을 차지하는 색 계열을 전부 배경으로 삼아야 한다. 단일 중앙값으로 바꿨을 때 빈칸 판정이 16/16 → 11/16으로 무너졌다.

### 10-3. 작고 어두운 템플릿 편향은 실재한다

`unalloyed_gold_needle`(신규 아티팩트)이 기준선 오답의 최다 출현 항목이다. 마스크 면적이 작고 어두운 템플릿은 어두운 배경 위에서 거의 항상 높은 점수를 받는다. §5 3-6의 정규화를 형식적으로 두지 말고 **반드시 구현하고 회귀로 검증**할 것.

### 10-4. 점유 판정은 어두운 아이콘에서 실패한다

전경 비율 임계값만으로는 **어두운 보라색 구체** 같은 아이콘을 빈칸으로 오판한다(`3.png` 슬롯 8, `5.png` 슬롯 30에서 실제 발생). 반대로 빈칸의 `+1`/`-1` 오버레이 텍스트는 전경으로 오인될 수 있다(`5.png` 슬롯 20). 밝기 기반 단일 임계값이 아니라 **배경 색 분포로부터의 거리 + 연결 성분 크기**를 함께 봐야 한다.

### 10-5. 프로토타입 실측 (참고)

호스트 프로토타입(전경분리 + 형태 IoU + 색 일치, 캘리브레이션 격자, 327 템플릿)으로 `2.png`에서:
- 빈칸 판정 **16/16 정확**
- 아이템 top-1 **약 85%** (20칸 중 17칸)

기준선 12.5% 대비 큰 폭의 개선이며, §5 Phase 3의 목표(≥99%)가 도달 가능한 범위임을 뒷받침한다. 다만 이는 한 장에 대한 육안 채점이므로 정식 지표가 아니다 — Phase 3에서 하네스로 정식 측정할 것.
